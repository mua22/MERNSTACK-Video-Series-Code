extends ../layout

block content
  h1 Login
  form(action="/login",method="post")        
        .form-group
            label(for="email") Email
            input#email(type="text", class="form-control", name="email" value="usman.akram@gmail.com")
        .form-group
            label(for="password") Password
            input#price(type="password", class="form-control", name="password", value="usman")
        input(type="submit", class="btn btn-info" value="Login")
